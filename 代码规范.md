# 代码规范

## 1. 总体要求

本次改造必须遵循以下原则：

- **优先复用现有项目结构**
- 不做无关重构
- 保留现有可用能力：配置读取、模型调用、翻译、分类、错误处理
- 新增数据库层、用户级聚合层、回写层
- 所有新增代码必须有类型标注
- 所有核心函数必须有 docstring
- 禁止把数据库配置、API Key、敏感账号名单硬编码在源码中

------

## 2. 推荐目录结构

```
project/
├─ main.py
├─ config.yaml
├─ .env
├─ requirements.txt
├─ app/
│  ├─ __init__.py
│  ├─ settings.py              # 配置加载
│  ├─ db.py                    # 数据库连接与 SQL 执行
│  ├─ models.py                # dataclass / pydantic 数据结构
│  ├─ repositories/
│  │  ├─ user_repository.py    # users_basic_info 读取/更新
│  │  ├─ tweets_repository.py  # users_tweets 读取
│  │  ├─ replies_repository.py # users_replies 读取
│  │  ├─ following_repository.py
│  │  └─ followers_repository.py
│  ├─ services/
│  │  ├─ text_cleaner.py
│  │  ├─ translator_service.py
│  │  ├─ rule_engine.py
│  │  ├─ llm_classifier.py
│  │  ├─ profile_generator.py
│  │  └─ user_analysis_service.py
│  ├─ prompts/
│  │  ├─ classify_user.txt
│  │  └─ profile_summary.txt
│  ├─ utils/
│  │  ├─ logger.py
│  │  ├─ retry.py
│  │  ├─ json_utils.py
│  │  └─ language.py
│  └─ constants.py
└─ tests/
   ├─ test_rule_engine.py
   ├─ test_profile_generator.py
   ├─ test_label_merge.py
   └─ test_db_update.py
```

------

## 3. 编码规范

### 3.1 Python 版本

- Python 3.10+

### 3.2 风格

- 遵循 PEP 8
- 使用 `black` 格式化
- 使用 `isort` 排序 import
- 使用 `ruff` 做静态检查
- 行宽建议 88 或 100

### 3.3 命名规范

- 文件名：`snake_case`
- 函数名：`snake_case`
- 类名：`PascalCase`
- 常量：`UPPER_SNAKE_CASE`

------

## 4. 类型与数据模型规范

### 4.1 必须使用类型标注

例如：

```
def analyze_user(user_id: int) -> dict[str, str | list[str]]:
    ...
```

### 4.2 推荐使用 dataclass / Pydantic

至少定义：

- `BasicUserInfo`
- `TweetRecord`
- `ReplyRecord`
- `FollowingRecord`
- `FollowerRecord`
- `UserAnalysisInput`
- `UserAnalysisResult`

------

## 5. 配置规范

### 5.1 配置来源

- 业务配置：`config.yaml`
- 敏感信息：`.env`

### 5.2 严禁硬编码

以下内容严禁出现在源码常量里：

- 数据库地址
- 数据库用户名/密码
- API Key
- 敏感账号名单

### 5.3 配置加载

统一通过 `settings.py` 读取，不允许在各模块直接散读 YAML/ENV。

------

## 6. 数据库访问规范

### 6.1 数据库访问层分离

禁止在业务层直接拼 SQL。
 所有 SQL 都必须放在 `repositories/` 中统一管理。

### 6.2 参数化查询

必须使用参数化 SQL，禁止字符串拼接，防 SQL 注入。

错误示例：

```
sql = f"SELECT * FROM users_basic_info WHERE id = {user_id}"
```

正确示例：

```
sql = "SELECT * FROM users_basic_info WHERE id = %s"
cursor.execute(sql, (user_id,))
```

### 6.3 事务要求

写回 `user_category` 和 `user_profile_summary` 时必须在同一事务中完成：

- 成功则提交
- 失败则回滚

### 6.4 批处理

读取用户时必须支持分页或分批：

- 不能一次性把全表全部载入内存

------

## 7. 业务逻辑规范

### 7.1 分层

业务必须分层，不允许把所有逻辑堆在 `main.py`：

- `repositories`：取数/写库
- `services`：分析逻辑
- `main.py`：流程编排

### 7.2 单一职责

每个服务做一件事：

- `rule_engine.py`：规则判定
- `llm_classifier.py`：模型判定
- `profile_generator.py`：画像文本生成
- `user_analysis_service.py`：统一编排

### 7.3 标签合并逻辑集中管理

只能有一个统一函数负责标签合并和互斥处理，例如：

```
def normalize_labels(raw_labels: list[str]) -> list[str]:
    ...
```

规则：

- 去重
- 过滤非法标签
- 固定顺序输出
- 处理“无敏感倾向”互斥

------

## 8. 文本处理规范

### 8.1 清洗

分析前必须统一清洗文本：

- 去除前后空白
- 把 `None` 转为空
- 去除超长重复字符
- 保留原始文本副本

### 8.2 非中文翻译

沿用现有翻译能力，但要做成可配置开关。

### 8.3 截断

单条文本过长时必须截断，避免模型输入爆掉。

### 8.4 去重

同一用户的重复文本应去重后再送模型。

------

## 9. Prompt 与模型调用规范

### 9.1 Prompt 外置

Prompt 必须写入 `app/prompts/`，不要硬编码在 Python 文件里。

### 9.2 模型输出必须是 JSON

调用模型时，必须要求其返回严格 JSON，代码里要做校验。

### 9.3 容错

模型输出异常时：

- 先做一次修复解析
- 修复失败则记录错误
- 不得让整个批任务中断

### 9.4 温度

分类任务模型温度建议低值，如 `0` 或 `0.1`

------

## 10. 用户画像生成规范

### 10.1 不得编造

画像只能使用真实字段和真实分析结果。

### 10.2 长度控制

建议控制在 80～220 字之间，避免过长。

### 10.3 输出风格

风格要求：

- 客观
- 简洁
- 信息密度高
- 不输出“可能是坏人”这类主观、口语化表述
- 用“当前可见数据表明”“账号呈现出”“未发现明显”这类表达

------

## 11. 日志规范

### 11.1 必须使用 logging

禁止 `print` 作为正式日志。

### 11.2 日志分级

- `INFO`：流程进度
- `WARNING`：字段缺失、部分数据为空
- `ERROR`：模型失败、数据库失败、JSON 解析失败

### 11.3 日志内容

至少记录：

- 当前用户 id / 账号
- 抽样条数
- 规则命中情况
- 模型返回标签
- 回写是否成功
- 错误堆栈

------

## 12. 错误处理规范

### 12.1 单用户失败不影响全局

分析某个用户失败时：

- 记录日志
- 跳过当前用户
- 继续处理下一个用户

### 12.2 重试

以下操作建议支持重试：

- 数据库瞬时连接失败
- 模型接口超时
- 翻译接口超时

### 12.3 明确异常类型

不要直接 `except Exception: pass`

必须：

- 捕获具体异常
- 写明错误上下文
- 保留堆栈

------

## 13. 测试规范

至少补充以下测试：

1. **标签互斥测试**
   - 命中敏感标签时不能出现 `无敏感倾向`
2. **多标签排序测试**
   - 标签顺序固定输出
3. **涉政关注名单测试**
   - 命中配置账号时必须包含 `涉政`
4. **空数据测试**
   - 用户没有 tweets / replies 时也能生成合理结果
5. **数据库更新测试**
   - 能正确更新 `user_category` 和 `user_profile_summary`
6. **模型异常测试**
   - JSON 非法时能容错

------

## 14. 性能与可维护性要求

### 14.1 性能

- 支持批量处理
- 避免 N+1 查询失控
- 每个批次按用户拉取数据时尽量做索引友好的查询

### 14.2 可维护性

- 敏感账号名单配置化
- 标签配置化
- 可扩展新增标签
- 可扩展新增分析字段

### 14.3 兼容性

尽量保持与现有项目配置结构兼容，减少对原有运行方式的破坏。