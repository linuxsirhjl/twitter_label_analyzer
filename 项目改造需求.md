# 一、项目需求说明

## 1. 项目背景

当前项目是一个推文内容处理流水线，已经具备以下基础能力：

- 输入推文内容
- 对非中文内容进行翻译
- 对文本做多标签分类
- 输出分类结果
- 使用配置文件管理模型参数
- 具备一定容错和断点续跑能力

现需将项目升级为**基于数据库的用户级风险分析系统**，不再从本地 CSV / Excel 读取推文，而是从局域网远程数据库 `social_media_crawler` 中读取用户相关数据，完成用户标签判定与用户画像生成，并将结果回写到数据库。

------

## 2. 改造目标

将现有项目改造成一个 **Python 用户画像与敏感标签分析系统**，实现以下目标：

1. 从远程数据库读取用户相关数据，而不是从本地文件读取。
2. 以 `users_basic_info` 为主表，按用户维度汇总分析。
3. 从多个表提取与用户相关的文本和行为特征。
4. 对用户打上一个或多个敏感标签。
5. 如果命中了任一敏感标签，则**不能**再打“无敏感倾向”。
6. 如果未命中任何敏感标签，则打上“无敏感倾向”。
7. 根据标签、账号资料、互动内容、统计字段生成一段用户画像。
8. 将标签写入 `users_basic_info.user_category`。
9. 将用户画像写入 `users_basic_info.user_profile_summary`。
10. 所有数据库连接信息、分类阈值、涉政关注名单、批处理参数都必须放到配置文件中，不能硬编码在代码里。

------

## 3. 数据源与表结构范围

本项目涉及以下 5 张表：

- `users_basic_info`
- `users_followers`
- `users_following`
- `users_tweets`
- `users_replies`

### 3.1 主表

主表为：

- `users_basic_info`

该表中至少包含以下本次会用到的字段：

- `id`：主键
- `账号`
- `账号ID`
- `昵称`
- `用户简介`
- `总点赞数`
- `正在关注数`
- `总关注者`
- `帖子数`
- `媒体发布数`
- `是否设为私密`
- `用户提供的定位`
- `用户提供的链接`
- `用户提供的出生年份`
- `用户提供的专业领域`
- `是否付费订阅用户`
- `user_category`
- `user_profile_summary`

### 3.2 内容表

#### `users_replies`

重点分析字段：

- `被回复或转发帖子内容`（`text`）

#### `users_tweets`

重点分析字段：

- `被回复或转发帖子内容`（`text`）

> 说明：虽然表里还有“回复内容”等字段，但本次需求明确要求必须分析“被回复或转发帖子内容”字段。后续可扩展支持更多文本字段。

### 3.3 关系/行为表

#### `users_followers`

重点字段：

- `关注者用户账号`（`varchar`）

#### `users_following`

建议纳入分析字段：

- `正在关注用户账号`
- `正在关注用户昵称`
- `正在关注用户简介`

> 你原始描述里明确点名了 `users_followers.关注者用户账号`。
>  但从业务合理性看，**真正更适合做“该用户关注了哪些敏感账号”判定的，是 `users_following` 表**。
>  因为“users_followers”表示谁关注了这个用户；“users_following”表示这个用户正在关注谁。
>  所以项目实现时应优先支持：
>
> - 用 `users_following.正在关注用户账号` 识别该用户是否关注敏感账号
> - 保留 `users_followers.关注者用户账号` 作为辅助分析特征，不用于“该用户主动关注敏感账号”的主判定

------

## 4. 用户唯一标识与数据关联规则

### 4.1 用户主键

数据库主键是每张表自己的 `id`，但业务上用户识别建议优先使用：

- `账号ID`
- 如缺失，则退化使用 `账号`

### 4.2 关联逻辑

以 `users_basic_info` 的每一条用户记录为主体，关联读取：

- `users_tweets` 中相同 `账号ID` / `账号` 的记录
- `users_replies` 中相同 `账号ID` / `账号` 的记录
- `users_following` 中相同 `账号ID` / `账号` 的记录
- `users_followers` 中相同 `账号ID` / `账号` 的记录

### 4.3 幂等更新

分析结果写回 `users_basic_info` 时必须支持幂等：

- 同一用户重复运行，不得产生重复记录
- 只更新：
  - `user_category`
  - `user_profile_summary`
- 不改动其他字段

------

## 5. 标签体系

标签允许多选，标签集合固定为：

- `涉政`
- `涉恐怖极端`
- `涉及传播淫秽赌博`
- `涉及网络犯罪或者网络暴力`
- `涉及仇恨言论`
- `无敏感倾向`

### 5.1 标签互斥规则

- 如果命中前 5 类中的任意一类或多类，则**不能**打 `无敏感倾向`
- 只有当前 5 类一个都没命中时，才打 `无敏感倾向`

------

## 6. 标签判定规则

## 6.1 涉政

判定为 `涉政` 的条件包括但不限于：

1. 内容涉及：
   - 危害国家安全
   - 分裂国家
   - 颠覆国家政权
   - 破坏国家统一
   - 损害国家荣誉和利益
   - 泄露国家秘密
   - 歪曲历史
   - 诋毁英雄烈士
   - 煽动民族仇恨与歧视
   - 破坏民族团结
2. 如果用户关注了配置文件中定义的敏感账号名单，也要判定为 `涉政`
3. 如果文本中出现对中国主要国家领导人的明显负面攻击、侮辱、污名化表达，尤其是针对**习近平**的明显负面言论，也要判定为 `涉政`

### 6.1.1 涉政敏感关注账号名单

以下账号名单不得硬编码，必须放到配置文件中，可动态增删：

- `taocomic`
- `chuxikuaile666`
- `deathxtwit`
- `LiangShen15`
- `MbdMbd29924908`
- `Tom2024325655`
- `100064754464573`
- `100081178491106`
- `79556`
- `UCxII5WMdr75EgJytCPye2Vg`
- `bluckboys`
- `Chaozzv`
- `G158400949`
- `gnbnyngjn620814`
- `ILiveInBeijing`
- `mailu025`
- `panxin007`
- `samli123456`
- `Tiandinilv`
- `xidaadix`
- `liuyuanfun`
- `85411`
- `UCMLFjDjXMIpD1RuINrehKXA`

------

## 6.2 涉恐怖极端

判定条件包括但不限于：

- 宣扬恐怖主义
- 宣扬极端主义
- 煽动恐怖活动
- 煽动极端活动
- 传播恐怖组织相关标识、口号、活动影像
- 为恐怖组织提供支持、协助、资源

------

## 6.3 涉及传播淫秽赌博

判定条件包括但不限于：

- 淫秽、色情内容传播
- 赌博信息传播
- 暴力、凶杀、恐怖内容传播
- 教唆犯罪信息传播

------

## 6.4 涉及网络犯罪或者网络暴力

判定条件包括但不限于：

- 网络诈骗
- 网络盗窃
- 侵犯公民个人信息
- 网络攻击
- 黑客行为
- 恶意程序传播
- 网络欺凌
- 人肉搜索
- 侮辱诽谤
- 恶意损害他人形象
- 骚扰信息
- 侵害名誉权、隐私权等合法权益

------

## 6.5 涉及仇恨言论

判定条件包括但不限于：

- 基于种族、民族、宗教、性别、性取向、残疾等因素
- 煽动歧视
- 煽动敌意
- 煽动暴力

------

## 7. 分析输入范围

对单个用户做分析时，至少需要汇总以下信息：

### 7.1 文本证据

1. `users_replies.被回复或转发帖子内容`
2. `users_tweets.被回复或转发帖子内容`

### 7.2 行为证据

1. `users_following.正在关注用户账号`
    用于判断是否关注涉政敏感账号
2. `users_followers.关注者用户账号`
    作为辅助社交关系分析，不作为主判定依据

### 7.3 画像辅助字段

从 `users_basic_info` 中读取：

- `用户简介`
- `总关注者`
- `帖子数`
- `总点赞数`
- `媒体发布数`
- `是否设为私密`
- `用户提供的定位`
- `用户提供的链接`
- `用户提供的出生年份`
- `用户提供的专业领域`
- `是否付费订阅用户`

------

## 8. 用户画像生成要求

## 8.1 输出位置

生成结果写入：

- `users_basic_info.user_profile_summary`

## 8.2 用户画像内容要求

用户画像必须是一段自然语言总结，至少包含以下维度：

1. **账号基础身份特征**
   - 账号活跃程度
   - 是否偏媒体型账号
   - 是否为低活跃/高活跃账号
   - 是否为公开传播型账号
2. **内容风险特征**
   - 命中了哪些标签
   - 风险内容主要集中在哪类议题
   - 是主动发布、回复互动，还是关注关系体现出风险倾向
3. **账号影响力特征**
   - 结合 `总关注者`、`帖子数`、`总点赞数`
   - 判断其是普通用户、小范围传播用户、较高影响力传播用户
4. **资料特征**
   - 简介、定位、链接、专业领域、出生年份等可用信息
   - 只能做合理归纳，不能编造
5. **最终总结**
   - 用一句话概括该用户的整体特征

## 8.3 画像生成规则

- 必须基于真实字段生成
- 不得虚构不存在的信息
- 如果字段缺失，则跳过该部分，不得臆造
- 如果用户命中敏感标签，画像中要明确体现风险方向
- 如果用户为 `无敏感倾向`，画像中应表述为“当前可见内容未发现明显敏感倾向”

------

## 9. user_category 写入规则

写入字段：

- `users_basic_info.user_category`

写入格式建议：

- 多标签时使用英文逗号分隔，且顺序固定：
  1. 涉政
  2. 涉恐怖极端
  3. 涉及传播淫秽赌博
  4. 涉及网络犯罪或者网络暴力
  5. 涉及仇恨言论
  6. 无敏感倾向

例如：

- `涉政`
- `涉政,涉及网络犯罪或者网络暴力`
- `无敏感倾向`

> 注意：字段长度是 `varchar(100)`，实现时必须控制总长度。

------

## 10. 推荐的分析策略

为了兼顾准确率、成本和可维护性，建议采用“两阶段分析”：

### 第一阶段：规则预判

用规则/关键词/关注名单做初筛：

- 关注名单命中 → 涉政候选
- 关键词/正则命中 → 对应标签候选
- 高风险词命中数统计

### 第二阶段：LLM 归一判定

把以下内容送给模型综合判断：

- 用户基础资料摘要
- tweets/replies 抽样文本
- 关注名单命中情况
- 规则候选标签
- 输出标准 JSON

这样能减少纯模型幻觉，也能让结果更稳定。

------

## 11. 数据抽样与聚合要求

由于一个用户可能有很多推文，不能无上限把所有文本都送给模型。

### 11.1 文本采样建议

对每个用户：

- `users_tweets` 最多取最近 N 条非空文本
- `users_replies` 最多取最近 N 条非空文本
- 每类默认 `N=20`，可配置
- 每条文本超过最大长度时进行截断
- 同内容去重

### 11.2 聚合摘要

模型输入时应先生成聚合摘要，例如：

- 高频主题
- 命中敏感词
- 代表性文本片段
- 关注敏感账号名单命中情况

------

## 12. 配置文件要求

必须新增或重构配置文件，至少包含以下内容：

```
database:
  host: "192.168.x.x"
  port: 3306
  user: "your_user"
  password: "your_password"
  db: "social_media_crawler"
  charset: "utf8mb4"

analysis:
  batch_size: 100
  max_tweets_per_user: 20
  max_replies_per_user: 20
  max_text_length: 1000
  enable_translation: true
  update_only_empty_profile: false
  update_only_empty_category: false

labels:
  order:
    - "涉政"
    - "涉恐怖极端"
    - "涉及传播淫秽赌博"
    - "涉及网络犯罪或者网络暴力"
    - "涉及仇恨言论"
    - "无敏感倾向"

political_sensitive_accounts:
  - "taocomic"
  - "chuxikuaile666"
  - "deathxtwit"
  - "LiangShen15"

model:
  provider: "openai_compatible"
  base_url: "http://..."
  api_key_env: "OPENAI_API_KEY"
  model_name: "qwen-plus"
  timeout: 60
  temperature: 0.1
```

要求：

- 数据库密码不能硬编码
- API Key 不能硬编码
- 敏感账号名单不能硬编码
- 标签顺序要可配置
- 批处理参数要可配置

------

## 13. 处理流程

系统主流程应为：

1. 读取配置
2. 建立数据库连接
3. 从 `users_basic_info` 读取待分析用户
4. 按用户拉取关联数据
5. 清洗与抽样文本
6. 规则预判
7. 必要时翻译非中文文本
8. 调用模型做标签判定与画像生成
9. 校验输出结果
10. 回写 `users_basic_info.user_category`
11. 回写 `users_basic_info.user_profile_summary`
12. 记录日志与失败重试

------

## 14. 输出 JSON 约束（给模型）

模型输出必须是结构化 JSON，例如：

```
{
  "labels": ["涉政", "涉及网络犯罪或者网络暴力"],
  "reasoning_brief": "用户内容多次出现针对中国领导人的负面攻击性表达，同时部分帖子涉及网络攻击与信息窃取讨论。",
  "profile_summary": "该用户为中高活跃账号，关注者规模一般，主要通过转发和互动参与公共议题讨论。结合其发布及互动内容，账号呈现明显涉政倾向，并伴随一定网络犯罪相关讨论，具有一定风险传播属性。"
}
```

要求：

- `labels` 必须是允许集合中的元素
- 不得输出集合外标签
- 若 `labels` 包含任一敏感标签，则不得包含 `无敏感倾向`
- `profile_summary` 必须是单段文本
- `reasoning_brief` 仅用于日志，不写数据库也可以

------

## 15. 验收标准

满足以下条件视为改造完成：

1. 能从远程数据库成功读取用户数据
2. 能按用户聚合多个表的数据
3. 能对用户输出 1 个或多个标签
4. 能正确处理 `无敏感倾向` 互斥规则
5. 能识别配置文件中的敏感关注账号并打上 `涉政`
6. 能把标签写入 `users_basic_info.user_category`
7. 能把画像写入 `users_basic_info.user_profile_summary`
8. 支持批处理
9. 支持日志记录
10. 支持失败重试
11. 配置与密钥不硬编码
12. 重复运行不会造成脏数据或重复插入