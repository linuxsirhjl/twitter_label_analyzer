# yqyd 启动和测试指南

## 一、环境准备

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 2. 配置环境变量

复制配置文件模板：
```bash
cp .env.example .env
```

编辑 `.env` 文件，配置 vLLM 服务地址：
```bash
# vLLM API 地址（根据实际部署修改）
VLLM_BASE_URL=http://127.0.0.1:5001/v1

# vLLM 模型名称
VLLM_MODEL_NAME=Qwen3.5-35B-A3B-FP8

# LLM 推理参数
LLM_TEMPERATURE=0.1
LLM_MAX_TOKENS=1024

# OCR 配置（可选调整）
OCR_PDF_DPI=200
OCR_MAX_PAGES=5
```

## 二、启动服务

### 方式 1：开发模式（单进程）

```bash
python app.py
```

服务将在 `http://0.0.0.0:5000` 启动。

### 方式 2：生产模式（Gunicorn）

```bash
gunicorn -w 1 -b 0.0.0.0:5000 app:app --timeout 1800
```

参数说明：
- `-w 1`：单进程（避免多进程加载模型）
- `-b 0.0.0.0:5000`：监听地址和端口
- `--timeout 1800`：请求超时时间 30 分钟

## 三、测试服务

### 1. 健康检查

```bash
curl http://localhost:5000/health
```

预期返回：
```json
{
  "status": "healthy",
  "vllm_connected": true
}
```

### 2. 使用测试脚本

```bash
python test1.py
```

测试脚本会：
1. 上传测试文档（PDF/DOC/DOCX）
2. 调用 `/extract` 接口提取字段
3. 打印提取结果

### 3. 手动测试接口

```bash
curl -X POST http://localhost:5000/extract \
  -F "file=@测试文档.pdf"
```

预期返回 JSON 格式的提取结果：
```json
{
  "企业名称": "示例企业",
  "联系人": "张三",
  "联系电话": "13800138000",
  "IP地址": ["192.168.1.1"],
  ...
}
```

## 四、常见问题

### 1. vLLM 连接失败

**错误信息**：
```
Failed to connect to vLLM: Connection refused
```

**解决方法**：
- 检查 vLLM 服务是否启动
- 确认 `.env` 中的 `VLLM_BASE_URL` 配置正确
- 测试 vLLM 服务：`curl http://127.0.0.1:5001/v1/models`

### 2. OCR 处理超时

**错误信息**：
```
OCR processing timeout
```

**解决方法**：
- 降低 `OCR_PDF_DPI`（如 150）
- 减少 `OCR_MAX_PAGES`（如 3）
- 禁用图像预处理：`OCR_PREPROCESS=0`

### 3. 模型输出包含思考过程

**现象**：返回结果中包含 `<think>...</think>` 标签

**解决方法**：
- 确认 `llm_engine.py` 中已配置 `enable_thinking=False`
- 检查 vLLM 服务是否支持该参数

### 4. 企业名称提取错误

**现象**：企业名称包含表格标题、HTML 标签或域名

**解决方法**：
- 检查 `schema.py` 中的 `clean_company_name()` 函数
- 确认 `extract_company_from_filename()` 正确过滤域名

## 五、性能优化建议

### 1. OCR 优化

```bash
# 快速模式（牺牲部分质量）
export OCR_PDF_DPI=150
export OCR_MAX_PAGES=3
export OCR_PREPROCESS=0
```

### 2. vLLM 优化

- 使用量化模型（如 FP8）提升推理速度
- 调整 `max_model_len` 和 `gpu_memory_utilization`
- 启用 `--enable-prefix-caching` 缓存系统提示词

### 3. 并发处理

```bash
# 多进程模式（需要足够显存）
gunicorn -w 4 -b 0.0.0.0:5000 app:app --timeout 1800
```

## 六、监控和日志

### 查看服务日志

```bash
# 开发模式
tail -f logs/app.log

# Gunicorn 模式
tail -f gunicorn.log
```

### 监控 vLLM 性能

```bash
# 查看 vLLM 日志
tail -f vllm.log

# 监控 GPU 使用率
nvidia-smi -l 1
```

## 七、部署检查清单

- [ ] 安装所有依赖包
- [ ] 配置 `.env` 文件
- [ ] 启动 vLLM 服务
- [ ] 测试 vLLM 连接
- [ ] 启动 Flask 服务
- [ ] 运行健康检查
- [ ] 执行测试脚本
- [ ] 验证提取结果准确性
- [ ] 配置日志轮转
- [ ] 设置进程守护（systemd/supervisor）
